# hotel-sales-mysql
 Small MySQL project: hotelsalesdb schema and queries
 # 🏨 Hotel Sales Analysis using MySQL

## 📌 Project Name
**hotel-sales-mysql**

## 📖 Project Description
This project focuses on analyzing **hotel sales data** using **MySQL**.  
The main objective is to solve different business-related queries based on hotel sales records by using **SQL queries**.

The project demonstrates the practical use of:
- Joins
- Aggregate functions
- Filtering and grouping
- Other important MySQL functions

This project is useful for understanding how SQL is applied in real-world **hotel management and sales analysis** scenarios.

---

## 🛠️ Technology Used
- **MySQL**
- SQL (Structured Query Language)

---

## 🔑 Key Concepts Used
- `SELECT` statements  
- `WHERE` conditions  
- `JOIN` (INNER JOIN, LEFT JOIN, etc.)  
- Aggregate functions:
  - `SUM()`
  - `COUNT()`
  - `AVG()`
  - `MIN()`
  - `MAX()`
- `GROUP BY`
- `ORDER BY`
- Subqueries
- Other required MySQL functions

---

## 🗃️ Database Overview
The database contains hotel sales–related data such as:
- Customer details
- Room bookings
- Sales transactions
- Revenue information

Using this data, various analytical queries are solved to get meaningful insights.

---

## ▶️ How to Run the Project
1. Install **MySQL** on your system.
2. Open **MySQL Workbench** or any MySQL client.
3. Create a database:
   ```sql
   CREATE DATABASE hotel_sales;

